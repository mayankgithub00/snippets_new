<snippet>
	<content><![CDATA[
ll setbits(ll x)
{
    ll count = 0;
    while (x > 0)
    {
        if (x & 1) count++;
        x = (x >> 1);
    }
    return count;
}
]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>setbits</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<!-- <scope>source.python</scope> -->
</snippet>
